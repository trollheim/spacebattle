package org.trollheim.game.spacebattle.newengine.elements;

public class DetectedObjects extends WorldObject{
public DetectedObjects(WorldObject worldObject) {
        super(worldObject.id, worldObject.team);
    }
}
